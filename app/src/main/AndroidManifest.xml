<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApp"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".KotlinActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".VisitNetActivity" />
        <activity android:name=".DataBindingClass" />
        <activity android:name=".UseBaseVBActivity" />

        <meta-data
            android:name="design_width_in_dp"
            android:value="441" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="784" />


        <receiver android:name=".NotificationClickReceiver"></receiver>

    </application>

</manifest>